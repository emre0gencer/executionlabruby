<% content_for :title, "Edit #{@runtime.name} — Execution Lab" %>

<div class="mb-6">
  <%= link_to "← #{@runtime.name}", runtime_path(@runtime), class: "text-slate-400 hover:text-slate-200 text-sm transition-colors" %>
</div>

<div class="max-w-lg">
  <h1 class="text-2xl font-bold text-white mb-6">Edit Runtime</h1>
  <%= render "form", runtime: @runtime %>
</div>
